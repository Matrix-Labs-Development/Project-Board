name: Deployment Reuse

on:
  push:
    branches:
      - production
      
  workflow_dispatch:

jobs:
  notify:
    name: Run Deployment Reuse

    uses: Matrix-Labs-Development/matrix_Actions-Runners/.github/workflows/deploy.yml@production

    secrets: inherit
    
